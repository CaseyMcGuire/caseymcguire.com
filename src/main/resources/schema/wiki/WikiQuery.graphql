
extend type Query {
  wikiByName(name: String!): Wiki
}

type Wiki {
  id: ID!
  name: String!,
  sidebar: [WikiSidebarMenuItem!]! @skipcodegen
}

union WikiSidebarMenuItem = WikiFolder | WikiPage

type WikiFolder {
  id: ID!
  name: String!
  children: [WikiSidebarMenuItem!]! @skipcodegen
}

type WikiPage {
  id: ID!
  name: String!
  content: String!
}


