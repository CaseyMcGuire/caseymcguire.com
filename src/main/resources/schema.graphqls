type Mutation {
  register(email: String!, password: String!): Boolean!
}

type Post {
  content: String!
  id: String!
  title: String!
}

type Query {
  post(id: String!): Post!
  posts(page: Int!): [Post!]!
}
